[package]
name = "avila-monitor"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Nícolas Ávila <nicolas@avila.inc>"]
license = "MIT OR Apache-2.0"
description = "System monitoring and metrics collection"
repository = "https://github.com/avilaops/arxis"
keywords = ["monitoring", "metrics", "telemetry", "no-std"]
categories = ["development-tools::profiling", "no-std"]

[lib]
name = "avila_monitor"
path = "src/lib.rs"

[features]
default = []

[dependencies]
